# Keep project startup deterministic for fresh clones and CI-like restores.
# This site is a Quarto project; the old blogdown-specific startup hooks have
# been intentionally removed.

options(
  repos = c(CRAN = "https://cloud.r-project.org")
)
